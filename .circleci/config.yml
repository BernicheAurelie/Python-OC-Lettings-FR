version: 2.1

orbs:
  python: circleci/python@1.5.0

jobs:
  build-test-and-check:
    docker:
      - image: cimg/python:3.10.2

    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
          # app-dir: ~/project/package-directory/  # If your requirements.txt isn't in the root directory.
          # pip-dependency-file: test-requirements.txt  # if you have a different name for your requirements file, maybe one that combines your runtime and test requirements. 
      - run:
          name: make migrations
          command: |
            python manage.py makemigrations --settings=oc_lettings_site.settings
            python manage.py migrate --settings=oc_lettings_site.settings
      - run:
          name: Run tests
          # This assumes pytest is installed via the install-package step above
          command: pytest -vv tests/unitaries/test_generics_urls.py
      - run:
          name: Check Flake8
          command: flake8 --per-file-ignores='.\tests\unitaries\test_lettings.py:E501 .\oc_lettings_site\settings.py:E501'
workflows:
  tests:
    jobs:
      - build-test-and-check